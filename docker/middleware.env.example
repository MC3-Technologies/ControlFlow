# Example environment for Docker middleware container

# Lattice access (required for production config)
LATTICE_BEARER_TOKEN=replace_with_your_token
# Only required for sandboxes.* environments
SANDBOXES_TOKEN=replace_with_your_sandboxes_token
LATTICE_URL=replace_with_your_lattice_host

# Networking from container to host MAVSDK server
# On Windows/Mac use host.docker.internal; on Linux use the host IP
MAVSDK_SERVER_HOST=host.docker.internal

# Runtime toggles
LATTICE_USE_GRPC=false
# Prefer pip SDK (default); set true to use vendored SDK under lattice-sdk-python
LATTICE_SDK_LOCAL=false

# Metrics
METRICS_ENABLED=true
METRICS_PORT=9090

